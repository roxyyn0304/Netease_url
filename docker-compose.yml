services:
  netease-url:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "5001:5000"
    volumes:
      - C:\Users\roxyy\Downloads:/app/downloads
    restart: unless-stopped
